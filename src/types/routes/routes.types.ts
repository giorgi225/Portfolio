export enum RouteName {
  HOME = "home",
  ABOUT = "about",
  PROJECTS = "projects",
  SERVICES = "services",
  RESUME = "resume",
}

export enum RoutePath {
  HOME = "/",
  ABOUT = "/about",
  PROJECTS = "/projects",
  SERVICES = "/services",
  RESUME = "/resume",
}
